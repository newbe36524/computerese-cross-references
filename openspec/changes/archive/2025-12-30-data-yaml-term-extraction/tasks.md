## 1. Implementation

- [x] 1.1 创建 `data.yaml` 文件骨架（包含数据结构定义）
- [x] 1.2 开发 README.md 解析脚本，提取 A-Z 字母分组的术语表格
- [x] 1.3 实现脚注标记解析（提取 `<sup>n</sup>` 格式的脚注引用）
- [x] 1.4 实现脚注定义解析（提取文档末尾的 `[1]`-`[6]` 脚注内容）
- [x] 1.5 将解析后的术语数据写入 `data.yaml`（按字母分组）
- [x] 1.6 将脚注数据写入 `data.yaml` 的 `footnotes` 部分
- [x] 1.7 验证生成的 `data.yaml` 文件格式正确性

## 2. Validation

- [x] 2.1 编写 YAML 格式验证脚本
- [x] 2.2 验证术语数量与 README.md 一致
- [x] 2.3 验证脚注引用完整性（引用的脚注编号在定义中存在）
- [x] 2.4 验证每个术语包含必需字段（word, meaning）
- [x] 2.5 验证字母分组完整性（A-Z 全部覆盖）

## 3. Documentation

- [x] 3.1 在 `data.yaml` 文件头部添加注释说明数据结构
